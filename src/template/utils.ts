// ShadowMode defines whether or not the shadow root's internal features 
// are accessible from JavaScript
// https://developer.mozilla.org/en-US/docs/Web/API/ShadowRoot/mode
export enum ShadowMode {
    open = "open",
    closed = "closed",
}
