export class Cart {
    constructor() {
    }

    /**
     * add (or update) cart qty for given sku
     *
     * For comparison see `Snipcart.api.cart.items.add` method
     * https://docs.snipcart.com/v3/sdk/api#add
     *
     * @param sku
     * @param qty
     */
    public add(sku: string, qty: number) {

    }
}
