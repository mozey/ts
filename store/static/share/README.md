TypeScript source code to share with other projects go in here, 
e.g. type definitions

The code in `static/src` must be excluded in `vuecart/tsconfig.json`,
otherwise the `npm run serve` command errors. Besides,
it's just a good idea to keep the namespace and component code separate

